bucket       = "infra-skill-trail-terraform-state"
key          = "infra-skill-trail/dev/terraform.tfstate"
region       = "ap-northeast-1"
encrypt      = true
use_lockfile = true

# Uncomment if the whole team uses the same AWS CLI profile name.
# profile = "terraform-user"
